using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //selam. sonunda olayý çözdük

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
